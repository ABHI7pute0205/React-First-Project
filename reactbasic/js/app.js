/*
 types of the export : 
    1. named export --> kon tya pan variable kashya la pan name del and te export kel   tya la named export . 
*/
// this is called named export and the problem with named export is that we need to pass the name in a import which we pass in a export mhanje export madhe variable , array la je name del te ch name import madhe pan pahije

export const arr = [10, 20, 30, 40];

const user = "reactjs";

export default user;
